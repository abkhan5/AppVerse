<UserControl x:Class="AppVerse.Desktop.GameOfLife.Views.GameOfLifeInformationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppVerse.Desktop.GameOfLife.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid ShowGridLines="True">
        <Grid.RowDefinitions >
            <RowDefinition Height="1" />
            <RowDefinition Height="10*" />
            <RowDefinition Height="3" />
            <RowDefinition Height="10*" />
            <RowDefinition Height="1" />
        </Grid.RowDefinitions>

        <TextBlock TextAlignment="Center"  Style="{DynamicResource ResourceKey=HeaderTextBlock}" Text="{Binding Title}" Grid.Row="1" />


        <TextBlock Width="1000" Text="{Binding OverView}" Grid.Row="3" TextWrapping="WrapWithOverflow"/>



    </Grid>
</UserControl>
